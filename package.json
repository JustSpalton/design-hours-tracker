{
  "name": "design-hours-tracker-firebase",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/vendor && cp index.html *.js *.css donaldson-logo.jpg dist/ && cp node_modules/xlsx/dist/xlsx.full.min.js dist/vendor/xlsx.full.min.js"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
